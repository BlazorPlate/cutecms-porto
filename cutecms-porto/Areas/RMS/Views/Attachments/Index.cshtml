@model IEnumerable<cutecms_porto.Areas.RMS.Models.DBModel.Attachment>
@{
    ViewBag.MainTitle = Resources.Resources.Attachments;
    ViewBag.Title = Resources.Resources.Index;
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Applicant.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FilePath)
        </th>
        <th></th>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Applicant.FullName)
        </td>
        <td>
            @Html.ActionLink(Resources.Resources.Download, "Download", new { FilePath = item.FilePath, FileName = item.FileName })
        </td>
        @*<td>
            @Html.ActionLink(Resources.Resources.Edit, "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink(Resources.Resources.Details, "Details", new { /* id=item.PrimaryKey */ }) |
             @Html.ActionLink(Resources.Resources.Delete, "Delete", new { /* id=item.PrimaryKey */ })
        </td>*@
    </tr>
}
</table>